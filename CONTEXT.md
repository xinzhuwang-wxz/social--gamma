# 社交森林 领域语言

以「一次真实共同行动的生命周期」为基本单位的校园搭子社交产品。本文定义领域词汇表；写任何代码、issue、文案前先读。

## 核心概念

**种子（Seed）**：
尚未发生的行动愿望，经个人 Agent 澄清后的结构化行动需求：做什么、大致时间、大致地点、人数、对同行者的必要要求（区分「必要条件」与「可商量条件」）。
_Avoid_: 帖子、动态、交友宣言

**信使鸟 / 个人 Agent（Courier）**：
每位用户的 AI 信使。负责澄清种子、寻找匹配、把合适的种子带回信箱。只起草、只建议，不代表用户发布或承诺。
_Avoid_: 数字分身、代答机器人

**A2A 对话（Agent-to-Agent）**：
发布方与候选方 Agent 之间的预热对话：判断时间/地点/兴趣适配，找共同点，为真人破冰备料。不替用户承诺、不交换联系方式、不使用未授权信息、必须明确标注为 Agent 对话。
_Avoid_: 替人聊天、自动配对

**种子信箱（Mailbox）**：
候选人收到被投递种子的地方。完整展示行动信息、有限展示发起人信息，候选人选择「愿意参与」或「暂不感兴趣」。

**成局（Formation）**：
发起人从有意向的候选人中**亲自**选定一名同行者（MVP 为 1 对 1）。AI 只排序与解释，不做最终选择；不向候选人展示排名与竞争；拒绝文案只说「种子已找到同行者」。

**行动房间（Action Room）**：
成局后创建的事件空间：对方信息摘要卡、整理后的 A2A 摘要（可折叠）、真人聊天、事件 AI。

**事件 AI（Event AI）**：
属于事件（而非任一成员）的 AI。成局后只主动破冰一次；此后仅在用户点「推进」时协助：总结当前进度、整理有限选项、提取共识、建议形成行动约定。一次只处理一个卡点。
_Avoid_: 群聊机器人、项目管理器

**推进（Nudge）**：
用户主动点击触发的事件 AI 协助，处理眼前一个决策卡点，最多给少量选项并保留「继续聊聊」。

**行动约定（Pact）**：
把「我们可以一起做」变成「我们已明确怎么做」的约定卡：行动内容、时间、地点/集合方式、必要备注。AI 只整理双方已谈过的内容；双方确认后固定在房间顶部。

**植物（Plant）**：
一次事件的具象化表达，属于所有参与者。成长只依据事件跨过的真实阶段：种子→发芽（成局）→长叶（有效交流）→生长（要素渐定）→花苞（约定确认）→开花（行动完成）。
_Avoid_: 按消息数量成长、虚拟宠物

**完成确认（Completion）**：
行动发生后双方分别点「已完成」，均确认才进入回忆阶段。照片是可选回忆素材，不是完成证明。

**共同回忆（Shared Memory）**：
双方都确认完成、且都愿意再次组队时生成，进入双方森林。各自管理自己上传的内容；任一方不愿继续则不生成，且对方不知道是谁拒绝。

**森林 / 花园（Forest / Garden）**：
个人真实经历档案：展示真正完成过的行动、保存共同回忆、作为下一次行动的发起入口。
_Avoid_: 资料卡、标签画像

**结出新种子（Reseed）**：
从已完成植物发起「再约一次」：继承活动类型与原搭子，修改时间与条件，对方接受后创建新事件，旧经历不被覆盖。

## 事件状态机

草稿 → 匹配中 → 已成局/破冰中（发芽）→ 讨论中（长叶/生长）→ 已约好（花苞）→ 等待行动 → 已完成（开花）

## 技术红线

- AI 能力真实调用 LLM（火山方舟 ARK，OpenAI 兼容），不 stub、不 mock、不写死回复。
- 不自己造 IM、不自己造 ORM、不自己造 LLM 编排框架——组装成熟开源。
- 视觉遵循 `docs/social-forest-prd-v0.md` 的视觉规范：世界是手绘的，交互是清晰的。
